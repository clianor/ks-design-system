{
  "name": "@ks-design-system/utils",
  "version": "0.0.1",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc --emitDeclarationOnly && rollup -c && rimraf .rollup.cache",
    "watch": "rollup -cw",
    "clean": "rimraf ./dist && rimraf tsconfig.tsbuildinfo && rimraf .rollup.cache"
  },
  "files": [
    "dist"
  ],
  "private": true,
  "dependencies": {}
}
